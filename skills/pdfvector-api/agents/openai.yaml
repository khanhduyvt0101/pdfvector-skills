interface:
  display_name: "PDF Vector API"
  short_description: "Use PDF Vector public APIs from agents."
  brand_color: "#0EA5E9"
  default_prompt: "Use $pdfvector-api to parse a PDF with page-separated markdown and show the request code."

policy:
  allow_implicit_invocation: true
